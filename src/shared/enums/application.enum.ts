export enum application_status {
  applied = 'applied',
  in_review = 'in_review',
  interview = 'interview',
  offer = 'offer',
  rejected = 'rejected',
  withdrawn = 'withdrawn',
  no_response = 'no_response',
}
